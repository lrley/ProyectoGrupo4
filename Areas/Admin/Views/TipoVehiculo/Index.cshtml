@{
    ViewData["Title"] = "Tipo Vehiculo";
}


@if (TempData["Success"] != null)
{
    <div id="alert-success" class="alert alert-success text-center fw-bold">
        @TempData["Success"]
    </div>
}
@if (TempData["Error"] != null)
{
    <div id="alert-error" class="alert alert-danger text-center fw-bold">
        @TempData["Error"]
    </div>
}



<div class="container mt-4">
    <div class="row">

        <div class="col-8">
            <h2 class="text-dark">Lista de Tipos de Vehiculos</h2>
        </div>

        <div class="col-4 text-right">
            <a asp-action="Create" class="btn btn-primary bg-dark">Crear Nueva Tipo Vehiculo</a>
            <i class="fa-solid fa-plus"></i>
        </div>

    </div>

    <br />
    <br />

    <table id="tblTipoVehiculo" class=" table table-striped table-bordered ">
        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Nombre del Tipo Vehiculo</th>
                <th>Fecha de Creacion</th>
                <th>Fecha de Actualizacion</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts {
    <script src="~/js/TipoVehiculo.js"></script>

    <script>
        // Ocultar automáticamente después de 3 segundos (3000 ms)
        setTimeout(function () {
            var successAlert = document.getElementById("alert-success");
            if (successAlert) {
                successAlert.style.display = "none";
            }

            var errorAlert = document.getElementById("alert-error");
            if (errorAlert) {
                errorAlert.style.display = "none";
            }
        }, 1000);
    </script>

}